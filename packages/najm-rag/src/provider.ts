import type { RegisteredTool } from 'najm-mcp';
import { TOOL_PROVIDER, type ToolProvider } from 'najm-mcp';

export { TOOL_PROVIDER as RAG_TOOL_PROVIDER } from 'najm-mcp';
export type { ToolProvider as RagToolProvider } from 'najm-mcp';
